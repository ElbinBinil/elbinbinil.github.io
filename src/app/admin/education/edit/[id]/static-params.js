// Server component for generateStaticParams
export async function generateStaticParams() {
  // Return empty array since these are dynamic admin pages
  // that shouldn't be pre-rendered
  return [];
}
